# Phase 7 수정 사이클 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 재시작 잔상 버그 수정 + Enemy/Boss 피격 플래시가 스프라이트 외곽에 흰 사각형을 노출하는 렌더링 버그 수정

**Architecture:** Game.clearEntities() 메서드 추가로 재시작 시 엔티티 완전 초기화. 피격 플래시는 fillRect 대신 drawSpriteTinted를 사용해 스프라이트 픽셀에만 흰색 오버레이를 적용.

**Tech Stack:** Vanilla JS, HTML5 Canvas, Vitest

---

## 수정 파일 목록

- **Modify:** `core/Game.js` — clearEntities() 메서드 추가
- **Modify:** `main.js` — startGame() 진입 시 game.clearEntities() 호출
- **Modify:** `entities/Enemy.js` — 피격 플래시 로직 교체
- **Modify:** `entities/Boss.js` — 피격 플래시 로직 교체
- **Test:** `tests/Game.test.js` — clearEntities 테스트 추가

---

### Task 1: Game.clearEntities() 메서드 추가

**Files:**
- Modify: `core/Game.js`
- Test: `tests/Game.test.js`

- [ ] **Step 1: 실패 테스트 작성**

`tests/Game.test.js` 파일 끝(마지막 `});` 전)에 아래 테스트를 추가:

```js
it('clearEntities()는 모든 엔티티를 제거한다', () => {
  const e1 = { update: vi.fn(), render: vi.fn() };
  const e2 = { update: vi.fn(), render: vi.fn() };
  game.addEntity(e1);
  game.addEntity(e2);
  game.clearEntities();
  expect(game.entities).toHaveLength(0);
});
```

- [ ] **Step 2: 테스트 실행 → FAIL 확인**

```bash
npx vitest run tests/Game.test.js
```

Expected: `clearEntities is not a function` 에러로 FAIL

- [ ] **Step 3: Game.js에 clearEntities() 구현**

`core/Game.js`의 `removeEntity()` 메서드 바로 아래에 추가:

```js
clearEntities() {
  this.entities = [];
}
```

- [ ] **Step 4: 테스트 실행 → PASS 확인**

```bash
npx vitest run tests/Game.test.js
```

Expected: 전체 PASS (기존 7개 + 신규 1개 = 8개)

- [ ] **Step 5: 커밋**

```bash
git add core/Game.js tests/Game.test.js
git commit -m "feat: phase7 T1-a — Game.clearEntities() 메서드 추가"
```

---

### Task 2: startGame() 재시작 시 엔티티 초기화

**Files:**
- Modify: `main.js` (line 166–168 startGame 함수 시작 부분)

- [ ] **Step 1: main.js startGame() 수정**

`main.js`의 `startGame()` 함수 진입부를 아래와 같이 수정:

```js
function startGame() {
  game.clearEntities();          // ← 추가: 이전 게임 엔티티 전부 제거
  ctx.clearRect(0, 0, canvas.width, canvas.height);
  state = 'playing';
  // ... 이후 코드 동일
```

- [ ] **Step 2: 전체 테스트 → PASS 확인**

```bash
npx vitest run
```

Expected: 전체 GREEN (기존 테스트 깨지지 않음)

- [ ] **Step 3: 브라우저 수동 검증**

```
1. npx serve . 실행 후 브라우저에서 게임 시작
2. 적에게 사망
3. R키로 재시작
4. 이전 플레이어 잔상이 남지 않는지 확인
5. 몬스터가 새로 스폰되는지 확인
```

- [ ] **Step 4: 커밋**

```bash
git add main.js
git commit -m "feat: phase7 T1-b — startGame 재시작 시 엔티티 완전 초기화"
```

---

### Task 3: Enemy 피격 플래시 → drawSpriteTinted 교체

**Files:**
- Modify: `entities/Enemy.js` (render 메서드 내 hitFlashTimer 블록)

- [ ] **Step 1: Enemy.js render() 피격 플래시 블록 교체**

`entities/Enemy.js`의 `render(ctx)` 메서드에서 아래 블록을:

```js
// 피격 플래시 오버레이 (흰색)
if (this.hitFlashTimer > 0) {
  ctx.save();
  ctx.globalAlpha = 0.6;
  ctx.fillStyle = '#ffffff';
  ctx.beginPath();
  ctx.rect(this.x - this.width / 2, this.y - this.height / 2, this.width, this.height);
  ctx.clip();
  ctx.fillRect(this.x - this.width / 2, this.y - this.height / 2, this.width, this.height);
  ctx.restore();
}
```

아래 코드로 교체:

```js
// 피격 플래시 오버레이 — 스프라이트가 있으면 tinted, 없으면 사각형
if (this.hitFlashTimer > 0) {
  const sprite = PixelRenderer.BUG_SPRITES[this.type];
  if (sprite) {
    PixelRenderer.drawSpriteTinted(
      ctx, sprite,
      this.x - this.width / 2, this.y - this.height / 2,
      2, '#ffffff', 0.7
    );
  } else {
    ctx.save();
    ctx.globalAlpha = 0.6;
    ctx.fillStyle = '#ffffff';
    ctx.fillRect(this.x - this.width / 2, this.y - this.height / 2, this.width, this.height);
    ctx.restore();
  }
}
```

- [ ] **Step 2: 전체 테스트 → PASS 확인**

```bash
npx vitest run
```

Expected: 전체 GREEN

- [ ] **Step 3: 커밋**

```bash
git add entities/Enemy.js
git commit -m "feat: phase7 T2-a — Enemy 피격 플래시 drawSpriteTinted로 교체"
```

---

### Task 4: Boss 피격 플래시 → drawSpriteTinted 교체

**Files:**
- Modify: `entities/Boss.js` (render 메서드 내 hitFlashTimer 블록)

- [ ] **Step 1: Boss.js render() 피격 플래시 블록 교체**

`entities/Boss.js`의 `render(ctx)` 메서드에서 아래 블록을:

```js
// 피격 플래시
if (this.hitFlashTimer > 0) {
  ctx.save();
  ctx.globalAlpha = 0.5;
  ctx.fillStyle = '#ffffff';
  ctx.fillRect(this.x - this.width / 2, this.y - this.height / 2, this.width, this.height);
  ctx.restore();
}
```

아래 코드로 교체:

```js
// 피격 플래시 — 스프라이트 픽셀에만 흰색 오버레이
if (this.hitFlashTimer > 0) {
  if (BOSS_SPRITE) {
    PixelRenderer.drawSpriteTinted(
      ctx, BOSS_SPRITE,
      this.x - this.width / 2, this.y - this.height / 2,
      2, '#ffffff', 0.6
    );
  } else {
    ctx.save();
    ctx.globalAlpha = 0.5;
    ctx.fillStyle = '#ffffff';
    ctx.fillRect(this.x - this.width / 2, this.y - this.height / 2, this.width, this.height);
    ctx.restore();
  }
}
```

- [ ] **Step 2: 전체 테스트 → PASS 확인**

```bash
npx vitest run
```

Expected: 전체 GREEN

- [ ] **Step 3: 브라우저 수동 검증**

```
1. 몬스터 피격 시 흰 사각형이 아닌 스프라이트 모양으로 반짝이는지 확인
2. 보스 피격 시 흰 사각형이 아닌 보스 스프라이트 모양으로 반짝이는지 확인
3. 보스 2페이즈 진입 후 붉은 사각형이 아닌 스프라이트로 표시되는지 확인
```

- [ ] **Step 4: 커밋**

```bash
git add entities/Boss.js
git commit -m "feat: phase7 T2-b — Boss 피격 플래시 drawSpriteTinted로 교체"
```

---

## 완료 기준

- [ ] 재시작(R키) 후 이전 플레이어/몬스터 잔상 없음
- [ ] 몬스터 피격 시 스프라이트 외곽 흰 사각형 미노출
- [ ] 보스 피격 시 스프라이트 외곽 흰 사각형 미노출
- [ ] 보스 2페이즈 빨간 오버레이 사각형 미노출 (Phase 6 T5에서 이미 수정됨, 회귀 없는지 확인)
- [ ] `npx vitest run` 전체 GREEN
