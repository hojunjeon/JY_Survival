# Phase 5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Phase 5 수정 4개 + 추가 7개 — 버그 스폰 위치 수정, 게임오버 루프 고정, EventModal 개선, 보상 무기 5종 추가, 픽셀 스프라이트 3종 추가

**Architecture:** Subagent-Driven. 각 Task는 독립 단위. TDD 먼저(실패 테스트 작성 → 구현 → GREEN). 스프라이트/렌더 변경은 테스트 없이 직접 구현. 현재 273개 테스트 GREEN 유지 목표.

**Tech Stack:** Vanilla JS ES6 모듈, HTML5 Canvas, Vitest (jsdom), `npx vitest run`으로 실행

---

## 파일 변경 목록

| 파일 | 변경 유형 | Task |
|------|---------|------|
| `systems/WaveSystem.js` | 수정 — 플레이어 기준 스폰, 이벤트 모드 카운트 관리 | T1, T5 |
| `main.js` | 수정 — gameOverLoop, 보상 무기 지급, waveSystem 호출 업데이트 | T2, T5, T10 |
| `entities/Enemy.js` | 수정 — HP 조정, 특수 공격, 피격 효과 | T3, T4, T6 |
| `ui/EventModal.js` | 수정 — 560×360 레이아웃, NPC 스프라이트 표시 | T8 |
| `sprites/PixelRenderer.js` | 추가 — 보스·NPC 스프라이트 | T7 |
| `entities/Boss.js` | 수정 — 스프라이트 렌더 | T11 |
| `weapons/Git.js` | 신규 | T9 |
| `weapons/SQL.js` | 신규 | T9 |
| `weapons/JavaScript.js` | 신규 | T9 |
| `weapons/Django.js` | 신규 | T9 |
| `weapons/LinuxBash.js` | 신규 | T9 |
| `tests/WaveSystem.test.js` | 추가 | T1, T5 |
| `tests/Enemy.test.js` | 추가 | T3, T4, T6 |
| `tests/EventModal.test.js` | 추가 | T8 |
| `tests/weapons/Git.test.js` | 신규 | T9 |
| `tests/weapons/SQL.test.js` | 신규 | T9 |
| `tests/weapons/JavaScript.test.js` | 신규 | T9 |
| `tests/weapons/Django.test.js` | 신규 | T9 |
| `tests/weapons/LinuxBash.test.js` | 신규 | T9 |

---

## Task 1: WaveSystem — 플레이어 기준 스폰 위치 수정 (Fix 1)

**Files:**
- Modify: `systems/WaveSystem.js`
- Modify: `main.js:260` (`waveSystem.update(dt)` 호출부)
- Modify: `tests/WaveSystem.test.js`

**문제**: `_edgePosition()`이 캔버스 좌표(0~600) 기준으로 적을 스폰. 플레이어가 월드 중심(1000,1000)에 있으면 적이 화면 밖 (0~600) 영역에 스폰되어 갑자기 플레이어 앞에 나타남.

**수정 방향**: `update(dt, playerX, playerY)` — 플레이어 위치 인수 추가. `_edgePosition(playerX, playerY)` — 플레이어 뷰포트 엣지 기준으로 스폰.

- [ ] **Step 1: 실패 테스트 작성 — update() 플레이어 위치 반영**

`tests/WaveSystem.test.js` 맨 아래에 추가:

```js
describe('WaveSystem 플레이어 기준 스폰', () => {
  test('update(dt, playerX, playerY) — 스폰 위치가 플레이어 기준 뷰포트 엣지 근처여야 한다', () => {
    const ws = new WaveSystem({ spawnInterval: 1, canvasWidth: 600, canvasHeight: 600 });
    const playerX = 1000, playerY = 1000;
    // 타이머를 충분히 진행시켜 스폰 유도
    const enemies = ws.update(1.0, playerX, playerY);
    expect(enemies.length).toBeGreaterThan(0);
    for (const e of enemies) {
      // 스폰 위치가 플레이어에서 canvasHalf(300)+100=400 이상 거리여야 함
      // 또는 플레이어에서 canvasHalf+100보다 멀어야 함 (뷰포트 외부)
      const dist = Math.max(Math.abs(e.x - playerX), Math.abs(e.y - playerY));
      expect(dist).toBeGreaterThanOrEqual(300);
    }
  });

  test('playerX/Y 생략 시 기존 동작 (캔버스 엣지 기준) 유지', () => {
    const ws = new WaveSystem({ spawnInterval: 1, canvasWidth: 600, canvasHeight: 600 });
    const enemies = ws.update(1.0);
    expect(enemies.length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: 실패 확인**

```
npx vitest run tests/WaveSystem.test.js
```
Expected: FAIL (update는 playerX/Y 인수를 받지 않음)

- [ ] **Step 3: WaveSystem.js 수정**

`systems/WaveSystem.js` 전체 교체:

```js
import { createEnemy } from '../entities/Enemy.js';

const WAVE_TYPES = ['syntax_error', 'null_pointer', 'seg_fault'];

export class WaveSystem {
  constructor({ spawnInterval, canvasWidth, canvasHeight }) {
    this.spawnInterval = spawnInterval;
    this.canvasWidth = canvasWidth;
    this.canvasHeight = canvasHeight;
    this.elapsed = 0;
    this.eventMode = null;       // null | { type, remaining }
  }

  setEventEnemyType(type) {
    // 하위 호환 — setEventMode로 대체 예정 (Task 5에서 완전 교체)
    this.eventMode = { type, remaining: Infinity };
  }

  clearEventEnemyType() {
    this.eventMode = null;
  }

  update(dt, playerX = null, playerY = null) {
    this.elapsed += dt;
    if (this.elapsed >= this.spawnInterval) {
      this.elapsed -= this.spawnInterval;
      return this._spawnWave(playerX, playerY);
    }
    return [];
  }

  _spawnWave(playerX, playerY) {
    const count = 3;
    const spawned = [];
    for (let i = 0; i < count; i++) {
      const { x, y } = this._edgePosition(playerX, playerY);
      const type = this.eventMode?.type ?? WAVE_TYPES[Math.floor(Math.random() * WAVE_TYPES.length)];
      spawned.push(createEnemy(type, x, y));
    }
    return spawned;
  }

  _edgePosition(playerX, playerY) {
    const side = Math.floor(Math.random() * 4);

    if (playerX == null || playerY == null) {
      // 플레이어 위치 없을 때: 캔버스 엣지 기준 (기존 동작)
      const w = this.canvasWidth;
      const h = this.canvasHeight;
      switch (side) {
        case 0: return { x: 0, y: Math.random() * h };
        case 1: return { x: w, y: Math.random() * h };
        case 2: return { x: Math.random() * w, y: 0 };
        case 3: return { x: Math.random() * w, y: h };
      }
    }

    // 플레이어 기준 뷰포트 엣지 스폰 (100px 버퍼 추가)
    const hw = this.canvasWidth  / 2 + 100;
    const hh = this.canvasHeight / 2 + 100;
    const WORLD_W = 2000, WORLD_H = 2000;

    let x, y;
    switch (side) {
      case 0: // 왼쪽 엣지
        x = playerX - hw;
        y = playerY - hh + Math.random() * (hh * 2);
        break;
      case 1: // 오른쪽 엣지
        x = playerX + hw;
        y = playerY - hh + Math.random() * (hh * 2);
        break;
      case 2: // 위쪽 엣지
        x = playerX - hw + Math.random() * (hw * 2);
        y = playerY - hh;
        break;
      case 3: // 아래쪽 엣지
        x = playerX - hw + Math.random() * (hw * 2);
        y = playerY + hh;
        break;
    }

    // 월드 경계 클램프
    x = Math.max(0, Math.min(WORLD_W, x));
    y = Math.max(0, Math.min(WORLD_H, y));

    return { x, y };
  }
}
```

- [ ] **Step 4: main.js waveSystem.update 호출 수정**

`main.js` 260번째 줄 부근:

```js
// 기존
const newEnemies = waveSystem.update(dt);

// 변경
const newEnemies = waveSystem.update(dt, player.x, player.y);
```

- [ ] **Step 5: 테스트 GREEN 확인**

```
npx vitest run tests/WaveSystem.test.js
```
Expected: 모든 WaveSystem 테스트 PASS

- [ ] **Step 6: 전체 테스트 GREEN 확인**

```
npx vitest run
```
Expected: 기존 273개 + 새 2개 = 275개 이상 PASS

- [ ] **Step 7: 커밋**

```bash
git add systems/WaveSystem.js main.js tests/WaveSystem.test.js
git commit -m "fix: WaveSystem 플레이어 기준 뷰포트 엣지 스폰으로 수정"
```

---

## Task 2: 게임오버 루프 + 재시작 잔상 제거 (Fix 4)

**Files:**
- Modify: `main.js` (renderGameOver → gameOverLoop, startGame에 clearRect 추가)

- [ ] **Step 1: main.js game_over 상태 처리 수정**

`main.js`의 game.update 안에서:

```js
// 기존 (사망 감지 부분)
if (player.isDead) {
  state = 'game_over';
  game.stop();
  renderGameOver();
  return;
}
```

를:

```js
if (player.isDead) {
  state = 'game_over';
  game.stop();
  gameOverLoop();
  return;
}
```

- [ ] **Step 2: gameOverLoop 함수 추가**

`main.js`의 `renderGameOver` 함수 위에 추가:

```js
function gameOverLoop() {
  if (state !== 'game_over') return;
  ctx.clearRect(0, 0, canvas.width, canvas.height);
  renderGameOver();
  requestAnimationFrame(gameOverLoop);
}
```

- [ ] **Step 3: startGame() 진입 시 canvas 클리어 추가**

`startGame()` 함수 맨 첫 줄에 추가:

```js
function startGame() {
  ctx.clearRect(0, 0, canvas.width, canvas.height);
  state = 'playing';
  // ... 기존 코드
```

- [ ] **Step 4: 브라우저에서 수동 검증**

`npx serve .` 로 게임 실행 후:
1. 플레이어가 사망 → 'GAME OVER' 화면이 안정적으로 표시되는지 확인
2. R키 → 재시작 시 이전 프레임 잔상 없이 깨끗이 시작되는지 확인
3. [ R키로 재시작 ] 텍스트가 화면에 보이는지 확인

- [ ] **Step 5: 커밋**

```bash
git add main.js
git commit -m "fix: 게임오버 루프 개선 — rAF 루프 + 재시작 잔상 제거"
```

---

## Task 3: 기본 버그 체력 조정 (Add 4)

**Files:**
- Modify: `entities/Enemy.js` (ENEMY_STATS HP 변경)
- Modify: `tests/Enemy.test.js`

**Python 데미지(12) 기준:**
- syntax_error: 40 → 24 (2대)
- seg_fault: 100 → 48 (4대)
- indentation_error: 60 → 36 (3대)
- env_error: 130 → 48 (4대)
- null_pointer(20), heal_bug(15) 유지

- [ ] **Step 1: 실패 테스트 작성**

`tests/Enemy.test.js`에 추가:

```js
describe('ENEMY_STATS HP 조정', () => {
  test('syntax_error HP = 24', () => {
    const e = createEnemy('syntax_error', 0, 0);
    expect(e.hp).toBe(24);
    expect(e.maxHp).toBe(24);
  });

  test('seg_fault HP = 48', () => {
    const e = createEnemy('seg_fault', 0, 0);
    expect(e.hp).toBe(48);
  });

  test('indentation_error HP = 36', () => {
    const e = createEnemy('indentation_error', 0, 0);
    expect(e.hp).toBe(36);
  });

  test('env_error HP = 48', () => {
    const e = createEnemy('env_error', 0, 0);
    expect(e.hp).toBe(48);
  });

  test('null_pointer HP = 20 (유지)', () => {
    const e = createEnemy('null_pointer', 0, 0);
    expect(e.hp).toBe(20);
  });
});
```

- [ ] **Step 2: 실패 확인**

```
npx vitest run tests/Enemy.test.js
```
Expected: FAIL (HP 값이 기존 값임)

- [ ] **Step 3: Enemy.js ENEMY_STATS 수정**

`entities/Enemy.js`의 ENEMY_STATS:

```js
const ENEMY_STATS = {
  syntax_error:      { hp: 24,  speed: 80,  contactDamage: 10, flees: false, dropsHpItem: false },
  null_pointer:      { hp: 20,  speed: 140, contactDamage: 5,  flees: false, dropsHpItem: false },
  seg_fault:         { hp: 48,  speed: 40,  contactDamage: 25, flees: false, dropsHpItem: false },
  heal_bug:          { hp: 15,  speed: 150, contactDamage: 0,  flees: true,  dropsHpItem: true  },
  indentation_error: { hp: 36,  speed: 70,  contactDamage: 15, flees: false, dropsHpItem: false },
  env_error:         { hp: 48,  speed: 35,  contactDamage: 15, flees: false, dropsHpItem: false },
};
```

- [ ] **Step 4: 테스트 GREEN 확인**

```
npx vitest run tests/Enemy.test.js
```

- [ ] **Step 5: 전체 테스트 GREEN**

```
npx vitest run
```

- [ ] **Step 6: 커밋**

```bash
git add entities/Enemy.js tests/Enemy.test.js
git commit -m "feat: 기본 버그 체력 조정 — Python 12 기준 2/3/4대"
```

---

## Task 4: 몬스터 피격 색상 변화 효과 (Add 7)

**Files:**
- Modify: `entities/Enemy.js` (render() 메서드)
- Modify: `tests/Enemy.test.js`

**현재 상태**: `Enemy.hitFlashTimer` 필드 이미 존재 (takeDamage 시 0.1초 세팅). render()에는 실제 효과 없음.

**추가**: hitFlashTimer > 0이면 스프라이트 위에 흰색 오버레이. isDead 직후 빨간 오버레이.

- [ ] **Step 1: 실패 테스트 작성**

`tests/Enemy.test.js`에 추가:

```js
describe('Enemy 피격 효과', () => {
  test('takeDamage 후 hitFlashTimer가 0.1로 설정된다', () => {
    const e = createEnemy('syntax_error', 0, 0);
    e.takeDamage(10);
    expect(e.hitFlashTimer).toBe(0.1);
  });

  test('update(dt) 호출 시 hitFlashTimer 감소', () => {
    const e = createEnemy('syntax_error', 0, 0);
    e.takeDamage(5);
    e.update(0.05, 0, 0);
    expect(e.hitFlashTimer).toBeCloseTo(0.05);
  });

  test('HP 0이 되면 isDead = true', () => {
    const e = createEnemy('syntax_error', 0, 0);
    e.takeDamage(24);
    expect(e.isDead).toBe(true);
  });
});
```

- [ ] **Step 2: 실패 확인**

```
npx vitest run tests/Enemy.test.js
```
Expected: FAIL (일부 기존 테스트가 새 HP 기준을 사용하는지 확인)

- [ ] **Step 3: Enemy.js render() 수정**

`entities/Enemy.js`의 `render(ctx)` 메서드:

```js
render(ctx) {
  const sprite = PixelRenderer.BUG_SPRITES[this.type];
  if (sprite) {
    PixelRenderer.drawSprite(ctx, sprite, this.x - this.width / 2, this.y - this.height / 2, 2);
  } else {
    const colors = {
      syntax_error:      '#ff4444',
      null_pointer:      'rgba(180,180,255,0.7)',
      seg_fault:         '#884400',
      heal_bug:          '#44ff88',
      indentation_error: '#ffaa44',
      env_error:         '#4488ff',
      enemy:             '#ff8800',
    };
    ctx.fillStyle = colors[this.type] || colors.enemy;
    ctx.fillRect(this.x - this.width / 2, this.y - this.height / 2, this.width, this.height);
  }

  // 피격 플래시 오버레이
  if (this.hitFlashTimer > 0) {
    ctx.save();
    ctx.globalAlpha = 0.6;
    ctx.fillStyle = '#ffffff';
    ctx.fillRect(this.x - this.width / 2, this.y - this.height / 2, this.width, this.height);
    ctx.restore();
  }

  // 사망 시 빨간 오버레이
  if (this.isDead) {
    ctx.save();
    ctx.globalAlpha = 0.8;
    ctx.fillStyle = '#ff0000';
    ctx.fillRect(this.x - this.width / 2, this.y - this.height / 2, this.width, this.height);
    ctx.restore();
  }
}
```

- [ ] **Step 4: 전체 테스트 GREEN**

```
npx vitest run
```

- [ ] **Step 5: 커밋**

```bash
git add entities/Enemy.js tests/Enemy.test.js
git commit -m "feat: 몬스터 피격 색상 효과 — 흰색 플래시 + 사망 빨간 오버레이"
```

---

## Task 5: WaveSystem 이벤트 모드 — 소환 수 제한 (Add 5 일부)

**Files:**
- Modify: `systems/WaveSystem.js` (setEventMode, clearEventMode 추가)
- Modify: `main.js` (이벤트 발생 시 기존 몹 제거 + setEventMode 사용)
- Modify: `tests/WaveSystem.test.js`

**목표**:
- E1: IndentationError 정확히 15마리만 스폰
- E3: EnvError 정확히 3마리만 스폰
- 이벤트 발생 시 기존 일반 몹 전부 제거
- 이벤트 클리어 시 이벤트 몹 전부 제거 + 일반 웨이브 재개

- [ ] **Step 1: 실패 테스트 작성**

`tests/WaveSystem.test.js`에 추가:

```js
describe('WaveSystem 이벤트 모드', () => {
  test('setEventMode("indentation_error", 15) — 15마리 스폰 후 중단', () => {
    const ws = new WaveSystem({ spawnInterval: 1, canvasWidth: 600, canvasHeight: 600 });
    ws.setEventMode('indentation_error', 15);
    let total = 0;
    for (let i = 0; i < 20; i++) {
      // 매번 spawnInterval 경과 처리
      ws.elapsed = ws.spawnInterval;
      const batch = ws.update(0, 300, 300);
      total += batch.length;
    }
    expect(total).toBe(15);
  });

  test('setEventMode — 스폰된 적 타입이 설정된 타입이어야 한다', () => {
    const ws = new WaveSystem({ spawnInterval: 1, canvasWidth: 600, canvasHeight: 600 });
    ws.setEventMode('env_error', 3);
    ws.elapsed = ws.spawnInterval;
    const batch = ws.update(0, 300, 300);
    for (const e of batch) {
      expect(e.type).toBe('env_error');
    }
  });

  test('clearEventMode() 후 일반 웨이브 재개', () => {
    const ws = new WaveSystem({ spawnInterval: 1, canvasWidth: 600, canvasHeight: 600 });
    ws.setEventMode('env_error', 3);
    ws.clearEventMode();
    ws.elapsed = ws.spawnInterval;
    const batch = ws.update(0, 300, 300);
    for (const e of batch) {
      expect(['syntax_error', 'null_pointer', 'seg_fault']).toContain(e.type);
    }
  });
});
```

- [ ] **Step 2: 실패 확인**

```
npx vitest run tests/WaveSystem.test.js
```

- [ ] **Step 3: WaveSystem.js setEventMode / clearEventMode 추가**

`systems/WaveSystem.js`에서 `setEventEnemyType` / `clearEventEnemyType`을 교체:

```js
setEventMode(type, count) {
  this.eventMode = { type, remaining: count };
  this.elapsed = 0; // 이벤트 시작 시 타이머 리셋
}

clearEventMode() {
  this.eventMode = null;
}

// 하위 호환 별칭 (기존 테스트가 쓰는 메서드)
setEventEnemyType(type) {
  this.eventMode = { type, remaining: Infinity };
}

clearEventEnemyType() {
  this.eventMode = null;
}
```

`_spawnWave` 수정 — remaining 카운트 관리:

```js
_spawnWave(playerX, playerY) {
  if (this.eventMode && this.eventMode.remaining <= 0) return [];

  const maxCount = this.eventMode
    ? Math.min(3, this.eventMode.remaining)
    : 3;

  const spawned = [];
  for (let i = 0; i < maxCount; i++) {
    const { x, y } = this._edgePosition(playerX, playerY);
    const type = this.eventMode?.type ?? WAVE_TYPES[Math.floor(Math.random() * WAVE_TYPES.length)];
    spawned.push(createEnemy(type, x, y));
  }

  if (this.eventMode) {
    this.eventMode.remaining -= spawned.length;
  }

  return spawned;
}
```

- [ ] **Step 4: main.js 이벤트 발생 처리 수정**

`main.js`의 `event_triggered` 핸들러:

```js
if (n.type === 'event_triggered') {
  eventModal.show('triggered', n.event);
  paused = true;
  // 기존 몹 전부 제거
  for (const e of enemies) game.removeEntity(e);
  enemies = [];
  // 이벤트 모드 진입
  if (n.event === 'E1') waveSystem.setEventMode('indentation_error', 15);
  if (n.event === 'E3') waveSystem.setEventMode('env_error', 3);
}
```

`event_cleared` 핸들러:

```js
if (n.type === 'event_cleared') {
  eventModal.show('cleared', n.event);
  paused = true;
  waveSystem.clearEventMode();
  // 이벤트 몹 전부 제거
  enemies = enemies.filter(e => {
    if (e.type === 'indentation_error' || e.type === 'env_error') {
      game.removeEntity(e);
      return false;
    }
    return true;
  });
}
```

- [ ] **Step 5: 전체 테스트 GREEN**

```
npx vitest run
```

- [ ] **Step 6: 커밋**

```bash
git add systems/WaveSystem.js main.js tests/WaveSystem.test.js
git commit -m "feat: WaveSystem 이벤트 모드 — 소환 수 제한 + 이벤트 발생 시 기존 몹 제거"
```

---

## Task 6: 이벤트 몹 특수 공격 (Add 5 나머지)

**Files:**
- Modify: `entities/Enemy.js` (특수 공격 로직 추가)
- Modify: `main.js` (적 업데이트 시 pending shots 수집)
- Modify: `tests/Enemy.test.js`

**스펙**:
- `IndentationError`: shootCooldown=2.5s, 플레이어 방향 투사체 (speed=100, damage=8)
- `EnvError`: dashCooldown=3s, 플레이어 방향 0.4초 동안 speed=400 돌진

- [ ] **Step 1: 실패 테스트 작성**

`tests/Enemy.test.js`에 추가:

```js
describe('이벤트 몹 특수 공격', () => {
  test('indentation_error — shootCooldown 초과 시 pending shot 생성', () => {
    const e = createEnemy('indentation_error', 100, 100);
    // cooldown이 0이 되도록 충분히 update
    e.update(3.0, 200, 100); // 타겟 방향으로
    const shots = e.getAndClearPendingShots();
    expect(shots.length).toBeGreaterThan(0);
    expect(shots[0]).toHaveProperty('vx');
    expect(shots[0]).toHaveProperty('vy');
    expect(shots[0].damage).toBe(8);
  });

  test('indentation_error — getAndClearPendingShots() 는 호출 후 비워진다', () => {
    const e = createEnemy('indentation_error', 100, 100);
    e.update(3.0, 200, 100);
    e.getAndClearPendingShots();
    const shots2 = e.getAndClearPendingShots();
    expect(shots2.length).toBe(0);
  });

  test('env_error — dashCooldown 초과 시 isDashing = true', () => {
    const e = createEnemy('env_error', 100, 100);
    e.update(3.5, 200, 100);
    expect(e.isDashing).toBe(true);
  });

  test('일반 몹(syntax_error)은 getAndClearPendingShots 반환값이 항상 빈 배열', () => {
    const e = createEnemy('syntax_error', 0, 0);
    e.update(5.0, 100, 100);
    expect(e.getAndClearPendingShots()).toEqual([]);
  });
});
```

- [ ] **Step 2: 실패 확인**

```
npx vitest run tests/Enemy.test.js
```

- [ ] **Step 3: Enemy.js 수정 — 특수 공격 필드 + 메서드 추가**

`Enemy` 생성자에 추가:

```js
// 특수 공격 (이벤트 몹 전용)
this._pendingShots = [];
this.specialCooldown = this._initialSpecialCooldown();
this.isDashing = false;
this._dashTimer = 0;
this._normalSpeed = speed;
```

`Enemy` 클래스에 헬퍼 메서드 추가:

```js
_initialSpecialCooldown() {
  if (this.type === 'indentation_error') return 2.5;
  if (this.type === 'env_error') return 3.0;
  return Infinity;
}

getAndClearPendingShots() {
  const shots = [...this._pendingShots];
  this._pendingShots = [];
  return shots;
}
```

`update(dt, targetX, targetY)` 수정 — 특수 공격 로직 추가:

```js
update(dt, targetX, targetY) {
  if (this.hitFlashTimer > 0) this.hitFlashTimer -= dt;
  if (dt === 0) return;

  const dx = targetX - this.x;
  const dy = targetY - this.y;
  const dist = Math.sqrt(dx * dx + dy * dy);
  if (dist === 0) return;

  const dirX = dx / dist;
  const dirY = dy / dist;

  // EnvError 돌진 처리
  if (this.type === 'env_error') {
    this.specialCooldown -= dt;
    if (this.isDashing) {
      this._dashTimer -= dt;
      if (this._dashTimer <= 0) {
        this.isDashing = false;
        this.speed = this._normalSpeed;
      }
    } else if (this.specialCooldown <= 0) {
      this.isDashing = true;
      this._dashTimer = 0.4;
      this.speed = 400;
      this.specialCooldown = 3.0;
    }
  }

  // IndentationError 총알 발사
  if (this.type === 'indentation_error') {
    this.specialCooldown -= dt;
    if (this.specialCooldown <= 0) {
      this.specialCooldown = 2.5;
      const speed = 100;
      this._pendingShots.push({
        x: this.x,
        y: this.y,
        vx: dirX * speed,
        vy: dirY * speed,
        damage: 8,
      });
    }
  }

  // 이동 처리
  if (this.flees) {
    this.x -= dirX * this.speed * dt;
    this.y -= dirY * this.speed * dt;
  } else {
    this.x += dirX * this.speed * dt;
    this.y += dirY * this.speed * dt;
  }
}
```

- [ ] **Step 4: main.js — 적 업데이트 시 pending shots 수집**

`main.js`의 "6. 적 이동" 블록 수정:

```js
// 6. 적 이동 + 특수 공격 투사체 수집
for (const enemy of enemies) {
  if (!enemy.isDead) {
    enemy.update(dt, player.x, player.y);
    // 이벤트 몹 특수 공격 투사체 수집
    const shots = enemy.getAndClearPendingShots();
    for (const s of shots) {
      const p = new Projectile(s.x, s.y, s.vx, s.vy, s.damage);
      p._isBossProjectile = true; // 플레이어 피격 처리 로직 재활용
      bossProjectiles.push(p);
      game.addEntity(p);
    }
  }
}
```

- [ ] **Step 5: 전체 테스트 GREEN**

```
npx vitest run
```

- [ ] **Step 6: 커밋**

```bash
git add entities/Enemy.js main.js tests/Enemy.test.js
git commit -m "feat: 이벤트 몹 특수 공격 — IndentationError 총알 + EnvError 돌진"
```

---

## Task 7: 픽셀 스프라이트 추가 — 보스·NPC (Add 2, Add 3)

**Files:**
- Modify: `sprites/PixelRenderer.js` (BOSS_SPRITE + NPC_SPRITES 추가)

> 스프라이트는 시각 자산이므로 단위 테스트 없이 직접 구현.

- [ ] **Step 1: PixelRenderer.js 파일 끝에 BOSS_SPRITE 추가**

`sprites/PixelRenderer.js`의 export 블록 직전에:

```js
// ─── 장선형 보스 스프라이트 (32×32, scale=2 → 64×64) ────────────────────────
// 페이즈1: 보라 상의 / 페이즈2: 빨간 상의 (렌더 시 tint로 처리)
const _B = null;
const BS = '#f5c5a3'; // 피부
const BH = '#5d4037'; // 갈색 단발 머리카락
const BE = '#1a1a1a'; // 눈
const BU = '#9b59b6'; // 보라 상의 (phase 1 기본)
const BD = '#7d3c98'; // 상의 어두운 부분
const BP = '#2c3e50'; // 바지
const BW = '#ffffff'; // 흰 셔츠 칼라
const BB = '#f5c5a3'; // 피부(손)

export const BOSS_SPRITE = [
  [_B,_B,_B,_B,_B,_B,_B,_B,_B, BH, BH, BH, BH, BH, BH,_B,_B, BH, BH, BH, BH, BH,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B, BH, BH, BH, BH, BH, BH, BH, BH, BH, BH, BH, BH, BH, BH, BH,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B, BH, BH, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BH, BH, BH,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B, BH, BH, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BH, BH,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B, BH, BS, BS, BS, BE, BE, BS, BS, BS, BS, BS, BE, BE, BS, BS, BS, BS, BH,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B, BH, BS, BS, BS, BE, BE, BS, BS, BS, BS, BS, BE, BE, BS, BS, BS, BS, BH,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B, BH, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BH,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B, BH, BS, BS, BS, BS, BS, BS, BS,'#e8a87c','#e8a87c', BS, BS, BS, BS, BS, BS, BS, BH,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B, BH, BH, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BS, BH, BH,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B, BH, BH, BH, BH, BS, BS, BS, BS, BS, BS, BH, BH, BH, BH,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B, BB, BW, BW, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BW, BW, BB,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B, BB, BW, BU, BU, BU, BD, BU, BU, BU, BU, BU, BU, BD, BU, BU, BU, BW, BB,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B, BB, BU, BU, BD, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BD, BU, BU, BB,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B, BB, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BB,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B, BB, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BB,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B, BB, BU, BD, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BD, BU, BB,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B, BB, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BB,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B, BB, BP, BP, BU, BU, BU, BU, BU, BU, BU, BU, BU, BU, BP, BP, BB,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B, BP, BP, BP, BP, BU, BU, BU, BU, BU, BU, BP, BP, BP, BP,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP, BP,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B, BP, BP, BP,_B,_B, BP, BP, BP, BP, BP,_B,_B, BP, BP,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B, BP, BP,_B,_B,_B, BP, BP, BP, BP, BP,_B,_B,_B, BP,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
  [_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B,_B],
];
```

- [ ] **Step 2: NPC 스프라이트 추가 (16×16)**

같은 파일 BOSS_SPRITE 아래에:

```js
// ─── NPC 스프라이트 (16×16, scale=3 → 48×48 EventModal 렌더) ──────────────
const _N = null;
const NS = '#f5c5a3';  // 피부
const NH = '#222222';  // 박진우 머리카락 (다크)
const NHB = '#8b6914'; // 이한정 머리카락 (브라운)
const NE2 = '#1a1a1a'; // 눈
const NU = '#4a90d9';  // SSAFY 유니폼 블루
const ND = '#2c5f8a';  // 유니폼 어두운 부분
const NP2 = '#1e3f5a'; // 바지

// 박진우 (남성, 검정 단발)
const JINU_SPRITE = [
  [_N,_N,_N, NH, NH, NH, NH, NH, NH, NH,_N,_N,_N,_N,_N,_N],
  [_N,_N, NH, NH, NH, NH, NH, NH, NH, NH, NH,_N,_N,_N,_N,_N],
  [_N,_N, NH, NS, NS, NS, NS, NS, NS, NS, NH,_N,_N,_N,_N,_N],
  [_N,_N, NH, NS, NE2,NS, NS, NE2,NS, NS, NH,_N,_N,_N,_N,_N],
  [_N,_N, NH, NS, NS, NS, NS, NS, NS, NS, NH,_N,_N,_N,_N,_N],
  [_N,_N, NH, NS, NS,'#e8a87c',NS, NS, NS, NS, NH,_N,_N,_N,_N,_N],
  [_N,_N,_N, NH, NH, NS, NH, NH,_N,_N,_N,_N,_N,_N,_N,_N],
  [_N, NS, NU, NU, NU, NU, NU, NU, NU, NS,_N,_N,_N,_N,_N,_N],
  [_N, NS, NU, ND, NU, NU, NU, ND, NU, NS,_N,_N,_N,_N,_N,_N],
  [_N, NS, NU, NU, NU, NU, NU, NU, NU, NS,_N,_N,_N,_N,_N,_N],
  [_N,_N, NU, NU, NU, NU, NU, NU, NU,_N,_N,_N,_N,_N,_N,_N],
  [_N,_N, NP2,NP2,NU, NU, NU, NP2,NP2,_N,_N,_N,_N,_N,_N,_N],
  [_N,_N, NP2,NP2,NP2,NP2,NP2,NP2,NP2,_N,_N,_N,_N,_N,_N,_N],
  [_N,_N, NP2,NP2,NP2,NP2,NP2,NP2,NP2,_N,_N,_N,_N,_N,_N,_N],
  [_N,_N, NP2,NP2,_N, NP2,NP2,_N, NP2,_N,_N,_N,_N,_N,_N,_N],
  [_N,_N,_N,_N,_N,_N,_N,_N,_N,_N,_N,_N,_N,_N,_N,_N],
];

// 이한정 (여성, 갈색 단발)
const HANJEONG_SPRITE = [
  [_N,_N,_N, NHB, NHB, NHB, NHB, NHB, NHB, NHB,_N,_N,_N,_N,_N,_N],
  [_N,_N, NHB, NHB, NHB, NHB, NHB, NHB, NHB, NHB, NHB,_N,_N,_N,_N,_N],
  [_N,_N, NHB, NS, NS, NS, NS, NS, NS, NS, NHB,_N,_N,_N,_N,_N],
  [_N,_N, NHB, NS, NE2,NS, NS, NE2,NS, NS, NHB,_N,_N,_N,_N,_N],
  [_N,_N, NHB, NS, NS, NS, NS, NS, NS, NS, NHB,_N,_N,_N,_N,_N],
  [_N, NHB, NHB, NS, NS,'#e8a87c',NS, NS, NS, NHB, NHB,_N,_N,_N,_N,_N],
  [_N, NHB,_N, NHB, NS, NHB, NHB,_N,_N,_N,_N,_N,_N,_N,_N,_N],
  [_N, NS, NU, NU, NU, NU, NU, NU, NU, NS,_N,_N,_N,_N,_N,_N],
  [_N, NS, NU, ND, NU, NU, NU, ND, NU, NS,_N,_N,_N,_N,_N,_N],
  [_N, NS, NU, NU, NU, NU, NU, NU, NU, NS,_N,_N,_N,_N,_N,_N],
  [_N,_N, NU, NU, NU, NU, NU, NU, NU,_N,_N,_N,_N,_N,_N,_N],
  [_N,_N, NP2,NP2,NU, NU, NU, NP2,NP2,_N,_N,_N,_N,_N,_N,_N],
  [_N,_N, NP2,NP2,NP2,NP2,NP2,NP2,NP2,_N,_N,_N,_N,_N,_N,_N],
  [_N,_N, NP2,NP2,NP2,NP2,NP2,NP2,NP2,_N,_N,_N,_N,_N,_N,_N],
  [_N,_N, NP2,NP2,_N, NP2,NP2,_N, NP2,_N,_N,_N,_N,_N,_N,_N],
  [_N,_N,_N,_N,_N,_N,_N,_N,_N,_N,_N,_N,_N,_N,_N,_N],
];

export const NPC_SPRITES = {
  '박진우': JINU_SPRITE,
  '이한정': HANJEONG_SPRITE,
};
```

- [ ] **Step 3: PixelRenderer의 export 블록 수정**

`sprites/PixelRenderer.js` 마지막 export 블록에 BOSS_SPRITE, NPC_SPRITES 추가:

```js
export class PixelRenderer {
  // ... 기존 내용 유지 ...
}

// 이미 export된 경우 아래만 추가
// BOSS_SPRITE, NPC_SPRITES는 위에서 이미 export 됨
```

> 주의: `BOSS_SPRITE`와 `NPC_SPRITES`는 각각 `export const`로 선언했으므로 별도 export 불필요.

- [ ] **Step 4: 커밋**

```bash
git add sprites/PixelRenderer.js
git commit -m "feat: 픽셀 스프라이트 추가 — 장선형 보스(32×32) + NPC 박진우·이한정(16×16)"
```

---

## Task 8: EventModal 개선 — 새 레이아웃 + NPC 스프라이트 + 진행 표시 (Fix 2 + Add 1)

**Files:**
- Modify: `ui/EventModal.js`
- Modify: `tests/EventModal.test.js`

**새 레이아웃 (560×360)**:
```
[ <E1> 이벤트 : 들여쓰기 지옥 ]
─────────────────────────────────
[스프라이트 48×48]  박진우
─────────────────────────────────
"아니 이거 또 이러네?"
"지윤님 저 진짜 들여쓰기 오류 때문에 미치겠습니다.."
─────────────────────────────────
박진우가 들여쓰기 오류로 인해 컴퓨터를 향해 주먹을 쥡니다.
그를 진정시키기 위해 IndentationError를 처치하세요 (0/15)
                                       [ Space ] 계속
```

- [ ] **Step 1: 실패 테스트 작성**

`tests/EventModal.test.js`에 추가:

```js
describe('EventModal 새 레이아웃', () => {
  test('show("triggered", "E1", {progress: "5/15"}) — visible=true, progress 저장', () => {
    const modal = new EventModal({ canvasWidth: 600, canvasHeight: 600 });
    modal.show('triggered', 'E1', { progress: '5/15' });
    expect(modal.visible).toBe(true);
    expect(modal.extra?.progress).toBe('5/15');
  });

  test('show("triggered", "E1") — extra 없이도 동작', () => {
    const modal = new EventModal({ canvasWidth: 600, canvasHeight: 600 });
    modal.show('triggered', 'E1');
    expect(modal.visible).toBe(true);
    expect(modal.extra).toEqual({});
  });

  test('hide() 후 visible=false', () => {
    const modal = new EventModal({ canvasWidth: 600, canvasHeight: 600 });
    modal.show('triggered', 'E1');
    modal.hide();
    expect(modal.visible).toBe(false);
  });
});
```

- [ ] **Step 2: 실패 확인**

```
npx vitest run tests/EventModal.test.js
```

- [ ] **Step 3: EventModal.js 전체 교체**

```js
import { PixelRenderer, NPC_SPRITES } from '../sprites/PixelRenderer.js';

const EVENT_CONFIG = {
  E1: {
    title: '이벤트 : 들여쓰기 지옥',
    character: '박진우',
    dialogueLines: [
      '아니 이거 또 이러네?',
      '지윤님 저 진짜 들여쓰기 오류 때문에 미치겠습니다..',
    ],
    situation: '박진우가 들여쓰기 오류로 인해 컴퓨터를 향해 주먹을 쥡니다.',
    actionBase: 'IndentationError를 처치하세요',
    progressFormat: (extra) => extra?.progress ? ` (${extra.progress})` : ' (0/15)',
    cleared: '들여쓰기 지옥 탈출 성공!',
    reward: '강화 재화 +2',
  },
  E3: {
    title: '이벤트 : 파이참 위기',
    character: '이한정',
    dialogueLines: [
      '어? 이거 뭐야. 아.. 파이참 또 환경변수 꼬였네.',
      '지윤님 이것 좀 풀어주세요.',
    ],
    situation: '이한정의 IDE가 먹통이 됐습니다.',
    actionBase: 'EnvError를 처치하고 60초를 생존하세요',
    progressFormat: () => '',
    cleared: '파이참 위기 극복!',
    reward: '무기 획득',
  },
};

export class EventModal {
  constructor({ canvasWidth, canvasHeight }) {
    this.cw = canvasWidth;
    this.ch = canvasHeight;
    this.visible = false;
    this.type = null;
    this.eventId = null;
    this.extra = {};
  }

  show(type, eventId, extra = {}) {
    this.visible = true;
    this.type = type;
    this.eventId = eventId;
    this.extra = extra;
  }

  hide() {
    this.visible = false;
  }

  render(ctx) {
    if (!this.visible) return;
    const cfg = EVENT_CONFIG[this.eventId];
    if (!cfg) return;

    const bw = 560;
    const bh = this.type === 'triggered' ? 360 : 200;
    const bx = (this.cw - bw) / 2;
    const by = (this.ch - bh) / 2;
    const accent = this.type === 'triggered' ? '#ffcc00' : '#4caf50';

    // 반투명 배경
    ctx.fillStyle = 'rgba(0,0,0,0.75)';
    ctx.fillRect(0, 0, this.cw, this.ch);

    // 모달 박스
    ctx.fillStyle = '#12122a';
    ctx.fillRect(bx, by, bw, bh);
    ctx.strokeStyle = accent;
    ctx.lineWidth = 2;
    ctx.strokeRect(bx, by, bw, bh);

    if (this.type === 'triggered') {
      this._renderTriggered(ctx, cfg, bx, by, bw, accent);
    } else {
      this._renderCleared(ctx, cfg, bx, by, bw, accent);
    }
  }

  _renderTriggered(ctx, cfg, bx, by, bw, accent) {
    let y = by + 36;

    // 제목
    ctx.fillStyle = accent;
    ctx.font = 'bold 16px monospace';
    ctx.textAlign = 'center';
    ctx.fillText(`[ <${this.eventId}> ${cfg.title} ]`, this.cw / 2, y);
    y += 20;

    // 구분선
    this._divider(ctx, bx, y, bw, accent);
    y += 20;

    // 캐릭터 스프라이트 + 이름
    const sprite = NPC_SPRITES?.[cfg.character];
    if (sprite) {
      PixelRenderer.drawSprite(ctx, sprite, bx + 24, y, 3); // 16×16 × 3 = 48×48
    }
    ctx.fillStyle = '#ffffff';
    ctx.font = 'bold 18px monospace';
    ctx.textAlign = 'left';
    ctx.fillText(cfg.character, bx + 80, y + 28);
    y += 60;

    // 구분선
    this._divider(ctx, bx, y, bw, accent);
    y += 22;

    // 대사
    ctx.fillStyle = '#ddeeff';
    ctx.font = '13px monospace';
    for (const line of cfg.dialogueLines) {
      ctx.fillText(`"${line}"`, bx + 24, y);
      y += 22;
    }
    y += 8;

    // 구분선
    this._divider(ctx, bx, y, bw, accent);
    y += 22;

    // 상황 설명
    ctx.fillStyle = '#cccccc';
    ctx.font = '12px monospace';
    ctx.fillText(cfg.situation, bx + 24, y);
    y += 22;

    // 액션 텍스트
    const action = cfg.actionBase + cfg.progressFormat(this.extra);
    ctx.fillStyle = '#ffee88';
    ctx.font = '13px monospace';
    ctx.fillText(action, bx + 24, y);

    // Space 힌트
    ctx.fillStyle = '#888888';
    ctx.font = '12px monospace';
    ctx.textAlign = 'right';
    ctx.fillText('[ Space ] 계속', bx + bw - 24, by + 360 - 20);
    ctx.textAlign = 'left';
  }

  _renderCleared(ctx, cfg, bx, by, bw, accent) {
    ctx.fillStyle = accent;
    ctx.font = 'bold 18px monospace';
    ctx.textAlign = 'center';
    ctx.fillText(`[${this.eventId}] ${cfg.cleared}`, this.cw / 2, by + 60);

    ctx.fillStyle = '#4fc3f7';
    ctx.font = '14px monospace';
    ctx.fillText(`보상: ${cfg.reward}`, this.cw / 2, by + 100);

    ctx.fillStyle = '#888888';
    ctx.font = '12px monospace';
    ctx.fillText('[ Space ] 계속', this.cw / 2, by + 170);
    ctx.textAlign = 'left';
  }

  _divider(ctx, bx, y, bw, color) {
    ctx.strokeStyle = color + '66';
    ctx.lineWidth = 1;
    ctx.beginPath();
    ctx.moveTo(bx + 16, y);
    ctx.lineTo(bx + bw - 16, y);
    ctx.stroke();
  }
}
```

- [ ] **Step 4: 전체 테스트 GREEN**

```
npx vitest run
```

- [ ] **Step 5: 커밋**

```bash
git add ui/EventModal.js tests/EventModal.test.js
git commit -m "feat: EventModal 개선 — 560×360 + NPC 스프라이트 + 새 레이아웃"
```

---

## Task 9: 보상 무기 5종 구현 (Add 6)

**Files:**
- Create: `weapons/Git.js`
- Create: `weapons/SQL.js`
- Create: `weapons/JavaScript.js`
- Create: `weapons/Django.js`
- Create: `weapons/LinuxBash.js`
- Create: `tests/weapons/Git.test.js`
- Create: `tests/weapons/SQL.test.js`
- Create: `tests/weapons/JavaScript.test.js`
- Create: `tests/weapons/Django.test.js`
- Create: `tests/weapons/LinuxBash.test.js`

**스펙**:
| 무기 | 쿨타임 | 방식 | 데미지 | fire() 반환 |
|------|--------|------|--------|-------------|
| Git | 4s | 광역 120px 범위 | 25 | `[{isAreaEffect:true, radius:120, damage:25, cx, cy}]` |
| SQL | 5s | 수직 줄기 3개 | 20 | Projectile 3개 배열 |
| JavaScript | 0.8s | 랜덤 5방향 | 8 | Projectile 5개 배열 |
| Django | 2s | 전방 부채꼴 5방향 | 15 | Projectile 5개 배열 |
| LinuxBash | 8s | 전체 화면 궁극기 | 60 | `[{isUlt:true, damage:60}]` |

**WeaponBase 구조 (기존)**:
- `this.cooldown`, `this.maxCooldown`, `this.level`
- `update(dt)` — cooldown 감소
- `canFire()` — cooldown <= 0
- `fire(x, y, dirX, dirY)` — 발사 후 cooldown 리셋

### Task 9a: Git 무기

- [ ] **Step 1: 실패 테스트 작성 (tests/weapons/Git.test.js)**

```js
import { GitWeapon } from '../../weapons/Git.js';

describe('GitWeapon', () => {
  test('초기 쿨타임 4초', () => {
    const w = new GitWeapon();
    expect(w.maxCooldown).toBe(4);
  });

  test('fire() — isAreaEffect 객체 반환', () => {
    const w = new GitWeapon();
    w.cooldown = 0;
    const result = w.fire(100, 200, 0, 1);
    expect(result).toHaveLength(1);
    expect(result[0].isAreaEffect).toBe(true);
    expect(result[0].radius).toBe(120);
    expect(result[0].damage).toBe(25);
    expect(result[0].cx).toBe(100);
    expect(result[0].cy).toBe(200);
  });

  test('fire() 후 쿨타임 리셋', () => {
    const w = new GitWeapon();
    w.cooldown = 0;
    w.fire(0, 0, 1, 0);
    expect(w.cooldown).toBe(4);
  });
});
```

- [ ] **Step 2: 실패 확인**

```
npx vitest run tests/weapons/Git.test.js
```

- [ ] **Step 3: weapons/Git.js 구현**

```js
import { WeaponBase } from './WeaponBase.js';

export class GitWeapon extends WeaponBase {
  constructor() {
    super({ name: 'Git', cooldown: 4 });
  }

  fire(x, y, dirX, dirY) {
    this.cooldown = this.maxCooldown;
    return [{ isAreaEffect: true, radius: 120, damage: 25, cx: x, cy: y }];
  }
}
```

- [ ] **Step 4: 테스트 GREEN**

```
npx vitest run tests/weapons/Git.test.js
```

### Task 9b: SQL 무기

- [ ] **Step 5: 실패 테스트 작성 (tests/weapons/SQL.test.js)**

```js
import { SQLWeapon } from '../../weapons/SQL.js';
import { Projectile } from '../../entities/Projectile.js';

describe('SQLWeapon', () => {
  test('초기 쿨타임 5초', () => {
    const w = new SQLWeapon();
    expect(w.maxCooldown).toBe(5);
  });

  test('fire() — Projectile 3개 반환', () => {
    const w = new SQLWeapon();
    w.cooldown = 0;
    const result = w.fire(300, 300, 0, -1);
    expect(result).toHaveLength(3);
    for (const p of result) {
      expect(p).toBeInstanceOf(Projectile);
      expect(p.damage).toBe(20);
    }
  });

  test('3개 투사체가 다른 x 위치에서 출발 (수직 줄기)', () => {
    const w = new SQLWeapon();
    w.cooldown = 0;
    const result = w.fire(300, 300, 0, -1);
    const xs = result.map(p => p.x);
    expect(new Set(xs).size).toBe(3); // 3개 모두 다른 x
  });
});
```

- [ ] **Step 6: 실패 확인**

```
npx vitest run tests/weapons/SQL.test.js
```

- [ ] **Step 7: weapons/SQL.js 구현**

```js
import { WeaponBase } from './WeaponBase.js';
import { Projectile } from '../entities/Projectile.js';

export class SQLWeapon extends WeaponBase {
  constructor() {
    super({ name: 'SQL', cooldown: 5 });
  }

  fire(x, y, dirX, dirY) {
    this.cooldown = this.maxCooldown;
    // 화면 전체 수직 줄기 3개 (x: 1/4, 2/4, 3/4 위치)
    const CANVAS_W = 600;
    const offsets = [-CANVAS_W / 4, 0, CANVAS_W / 4];
    return offsets.map(dx => new Projectile(x + dx, y, 0, -400, 20));
  }
}
```

- [ ] **Step 8: 테스트 GREEN**

```
npx vitest run tests/weapons/SQL.test.js
```

### Task 9c: JavaScript 무기

- [ ] **Step 9: 실패 테스트 작성 (tests/weapons/JavaScript.test.js)**

```js
import { JavaScriptWeapon } from '../../weapons/JavaScript.js';
import { Projectile } from '../../entities/Projectile.js';

describe('JavaScriptWeapon', () => {
  test('초기 쿨타임 0.8초', () => {
    const w = new JavaScriptWeapon();
    expect(w.maxCooldown).toBe(0.8);
  });

  test('fire() — Projectile 5개 반환', () => {
    const w = new JavaScriptWeapon();
    w.cooldown = 0;
    const result = w.fire(300, 300, 1, 0);
    expect(result).toHaveLength(5);
    for (const p of result) {
      expect(p).toBeInstanceOf(Projectile);
      expect(p.damage).toBe(8);
    }
  });

  test('5개 투사체가 서로 다른 방향 (speed 250 기준 각도 검증)', () => {
    const w = new JavaScriptWeapon();
    w.cooldown = 0;
    const result = w.fire(0, 0, 1, 0);
    const speeds = result.map(p => Math.sqrt(p.vx * p.vx + p.vy * p.vy));
    for (const s of speeds) {
      expect(s).toBeCloseTo(250, 0);
    }
  });
});
```

- [ ] **Step 10: 실패 확인**

```
npx vitest run tests/weapons/JavaScript.test.js
```

- [ ] **Step 11: weapons/JavaScript.js 구현**

```js
import { WeaponBase } from './WeaponBase.js';
import { Projectile } from '../entities/Projectile.js';

export class JavaScriptWeapon extends WeaponBase {
  constructor() {
    super({ name: 'JavaScript', cooldown: 0.8 });
  }

  fire(x, y, dirX, dirY) {
    this.cooldown = this.maxCooldown;
    const speed = 250;
    const projs = [];
    for (let i = 0; i < 5; i++) {
      const angle = Math.random() * Math.PI * 2;
      projs.push(new Projectile(x, y, Math.cos(angle) * speed, Math.sin(angle) * speed, 8));
    }
    return projs;
  }
}
```

- [ ] **Step 12: 테스트 GREEN**

```
npx vitest run tests/weapons/JavaScript.test.js
```

### Task 9d: Django 무기

- [ ] **Step 13: 실패 테스트 작성 (tests/weapons/Django.test.js)**

```js
import { DjangoWeapon } from '../../weapons/Django.js';
import { Projectile } from '../../entities/Projectile.js';

describe('DjangoWeapon', () => {
  test('초기 쿨타임 2초', () => {
    const w = new DjangoWeapon();
    expect(w.maxCooldown).toBe(2);
  });

  test('fire() — Projectile 5개 반환', () => {
    const w = new DjangoWeapon();
    w.cooldown = 0;
    const result = w.fire(300, 300, 0, -1);
    expect(result).toHaveLength(5);
    for (const p of result) {
      expect(p).toBeInstanceOf(Projectile);
      expect(p.damage).toBe(15);
    }
  });

  test('5개 투사체가 부채꼴 배치 (각도 범위 내)', () => {
    const w = new DjangoWeapon();
    w.cooldown = 0;
    // dirX=1, dirY=0 (오른쪽 발사)
    const result = w.fire(0, 0, 1, 0);
    for (const p of result) {
      const angle = Math.atan2(p.vy, p.vx);
      // 오른쪽 ±45도 이내
      expect(angle).toBeGreaterThanOrEqual(-Math.PI / 4 - 0.01);
      expect(angle).toBeLessThanOrEqual(Math.PI / 4 + 0.01);
    }
  });
});
```

- [ ] **Step 14: 실패 확인**

```
npx vitest run tests/weapons/Django.test.js
```

- [ ] **Step 15: weapons/Django.js 구현**

```js
import { WeaponBase } from './WeaponBase.js';
import { Projectile } from '../entities/Projectile.js';

export class DjangoWeapon extends WeaponBase {
  constructor() {
    super({ name: 'Django', cooldown: 2 });
  }

  fire(x, y, dirX, dirY) {
    this.cooldown = this.maxCooldown;
    const speed = 300;
    const baseAngle = Math.atan2(dirY, dirX);
    const spread = Math.PI / 4; // ±45도
    const count = 5;
    const projs = [];
    for (let i = 0; i < count; i++) {
      const angle = baseAngle + spread * (i / (count - 1) - 0.5) * 2;
      projs.push(new Projectile(x, y, Math.cos(angle) * speed, Math.sin(angle) * speed, 15));
    }
    return projs;
  }
}
```

- [ ] **Step 16: 테스트 GREEN**

```
npx vitest run tests/weapons/Django.test.js
```

### Task 9e: LinuxBash 무기

- [ ] **Step 17: 실패 테스트 작성 (tests/weapons/LinuxBash.test.js)**

```js
import { LinuxBashWeapon } from '../../weapons/LinuxBash.js';

describe('LinuxBashWeapon', () => {
  test('초기 쿨타임 8초', () => {
    const w = new LinuxBashWeapon();
    expect(w.maxCooldown).toBe(8);
  });

  test('fire() — isUlt 시그널 반환', () => {
    const w = new LinuxBashWeapon();
    w.cooldown = 0;
    const result = w.fire(0, 0, 1, 0);
    expect(result).toHaveLength(1);
    expect(result[0].isUlt).toBe(true);
    expect(result[0].damage).toBe(60);
  });

  test('fire() 후 쿨타임 8초로 리셋', () => {
    const w = new LinuxBashWeapon();
    w.cooldown = 0;
    w.fire(0, 0, 1, 0);
    expect(w.cooldown).toBe(8);
  });
});
```

- [ ] **Step 18: 실패 확인**

```
npx vitest run tests/weapons/LinuxBash.test.js
```

- [ ] **Step 19: weapons/LinuxBash.js 구현**

```js
import { WeaponBase } from './WeaponBase.js';

export class LinuxBashWeapon extends WeaponBase {
  constructor() {
    super({ name: 'LinuxBash', cooldown: 8 });
  }

  fire(x, y, dirX, dirY) {
    this.cooldown = this.maxCooldown;
    return [{ isUlt: true, damage: 60 }];
  }
}
```

- [ ] **Step 20: 전체 무기 테스트 GREEN**

```
npx vitest run tests/weapons/
```

- [ ] **Step 21: 전체 테스트 GREEN**

```
npx vitest run
```

- [ ] **Step 22: 커밋**

```bash
git add weapons/Git.js weapons/SQL.js weapons/JavaScript.js weapons/Django.js weapons/LinuxBash.js \
  tests/weapons/Git.test.js tests/weapons/SQL.test.js tests/weapons/JavaScript.test.js \
  tests/weapons/Django.test.js tests/weapons/LinuxBash.test.js
git commit -m "feat: 보상 무기 5종 구현 — Git/SQL/JavaScript/Django/LinuxBash"
```

---

## Task 10: 보상 무기 지급 시스템 — main.js 연동 (Fix 3 + Add 6 나머지)

**Files:**
- Modify: `main.js` (보상 무기 풀, E3/보스 클리어 시 지급, tryFireWeapon 확장, HUD 무기 목록)

- [ ] **Step 1: main.js import 추가**

`main.js` 상단 import 블록에 추가:

```js
import { GitWeapon }        from './weapons/Git.js';
import { SQLWeapon }        from './weapons/SQL.js';
import { JavaScriptWeapon } from './weapons/JavaScript.js';
import { DjangoWeapon }     from './weapons/Django.js';
import { LinuxBashWeapon }  from './weapons/LinuxBash.js';
```

- [ ] **Step 2: startGame() — 보상 무기 풀 + 보유 무기 배열 초기화**

`startGame()` 함수 내 `let enemies = [];` 블록 근처에 추가:

```js
// ── 보상 무기 시스템 ─────────────────────────────────────────────────────
const REWARD_WEAPON_POOL = [
  GitWeapon, SQLWeapon, JavaScriptWeapon, DjangoWeapon, LinuxBashWeapon,
];
const availableRewards = [...REWARD_WEAPON_POOL]; // 남은 보상 풀
let ownedWeapons = [selectedWeapon];              // 보유 무기 목록
let rewardNotice = null;                          // '무기 획득: XXX' 알림
let rewardNoticeTimer = 0;

function giveRewardWeapon() {
  if (availableRewards.length === 0) return;
  const idx = Math.floor(Math.random() * availableRewards.length);
  const WeaponClass = availableRewards.splice(idx, 1)[0];
  const weapon = new WeaponClass();
  if (ownedWeapons.length < 4) ownedWeapons.push(weapon);
  rewardNotice = `무기 획득: ${weapon.name}`;
  rewardNoticeTimer = 3;
}
```

- [ ] **Step 3: event_cleared 핸들러 — E3 보상 무기 지급**

`main.js`의 event_cleared 처리 (Task 5에서 수정한 블록) 안에 추가:

```js
if (n.type === 'event_cleared') {
  eventModal.show('cleared', n.event);
  paused = true;
  waveSystem.clearEventMode();
  // 이벤트 몹 제거 (Task 5에서 추가된 코드 유지)
  enemies = enemies.filter(e => { /* ... */ });
  // E3 보상 무기 지급
  if (n.event === 'E3') giveRewardWeapon();
}
```

- [ ] **Step 4: boss_killed 핸들러 — 보스 처치 시 보상 무기 지급**

`main.js`의 보스 처치 로직 (`boss.isDead` 감지 부분):

```js
if (boss.isDead) {
  bossDialogue = boss.getDialogue('death');
  bossDialogueTimer = 4;
  giveRewardWeapon(); // 보스 처치 보상 무기
  const killNotifs = eventSystem.notifyBossKill();
  // ... 기존 코드
}
```

- [ ] **Step 5: tryFireWeapon 확장 — isAreaEffect / isUlt 처리**

`main.js`의 `tryFireWeapon` 함수:

```js
function tryFireWeapon(weapon, player) {
  if (!weapon) return;
  if (weapon.name === 'Java') return;
  if (!weapon.canFire()) return;

  const dirX = player.lastDirX;
  const dirY = player.lastDirY;

  const newProjs = weapon.fire(player.x, player.y, dirX, dirY);
  for (const p of newProjs) {
    // 광역 효과 (Git)
    if (p.isAreaEffect) {
      for (const enemy of enemies) {
        if (!enemy.isDead) {
          const dx = enemy.x - p.cx;
          const dy = enemy.y - p.cy;
          if (Math.sqrt(dx * dx + dy * dy) <= p.radius) {
            enemy.takeDamage(p.damage);
          }
        }
      }
      continue;
    }
    // 궁극기 (LinuxBash)
    if (p.isUlt) {
      for (const enemy of enemies) {
        if (!enemy.isDead) enemy.takeDamage(p.damage);
      }
      if (boss && !boss.isDead) boss.takeDamage(p.damage);
      continue;
    }
    // 일반 투사체
    projectiles.push(p);
    game.addEntity(p);
  }
}
```

- [ ] **Step 6: 보유 무기 자동 발사 (ownedWeapons 순환)**

`game.update`의 "2. 무기 업데이트" 부분:

```js
// 2. 무기 업데이트 + 자동 발사 (보유 무기 전부)
for (const weapon of ownedWeapons) {
  if (weapon.name !== 'Java') weapon.update(dt);
  tryFireWeapon(weapon, player);
}
// Java 오비탈 별도 업데이트
const javaWeapon = ownedWeapons.find(w => w.name === 'Java');
if (javaWeapon) javaWeapon.update(dt);
```

> 기존 `if (selectedWeapon) { selectedWeapon.update(dt); tryFireWeapon(...) }` 대체.

- [ ] **Step 7: 무기 획득 알림 타이머 업데이트**

`game.update` 내 적절한 위치에 추가:

```js
// 무기 획득 알림 타이머
if (rewardNoticeTimer > 0) {
  rewardNoticeTimer -= dt;
  if (rewardNoticeTimer <= 0) rewardNotice = null;
}
```

- [ ] **Step 8: HUD에 보유 무기 목록 + 획득 알림 렌더**

`game.render` 내 기존 무기명 표시 부분 교체:

```js
// 보유 무기 슬롯 (우측 상단)
ownedWeapons.forEach((w, i) => {
  ctx.textAlign = 'right';
  ctx.fillStyle = '#4fc3f7';
  ctx.font = '13px monospace';
  ctx.fillText(`[${i + 1}] ${w.name}`, canvas.width - 12, 56 + i * 20);
});
ctx.textAlign = 'left';

// 무기 획득 알림
if (rewardNotice) {
  ctx.fillStyle = '#ffd700';
  ctx.font = 'bold 15px monospace';
  ctx.textAlign = 'center';
  ctx.fillText(rewardNotice, canvas.width / 2, canvas.height / 2 - 100);
  ctx.textAlign = 'left';
}
```

- [ ] **Step 9: 전체 테스트 GREEN**

```
npx vitest run
```

- [ ] **Step 10: 브라우저에서 수동 검증**

`npx serve .` 후:
1. E3 클리어 → 무기 획득 알림 3초 표시 + 보유 무기 목록에 추가되는지 확인
2. 보스 처치 → 무기 획득 알림 표시 확인
3. 새 무기 자동 발사 동작 확인 (Git 범위 공격, LinuxBash 전체 피격)

- [ ] **Step 11: 커밋**

```bash
git add main.js
git commit -m "feat: 보상 무기 지급 시스템 연동 — E3/보스 클리어 시 랜덤 무기 지급"
```

---

## Task 11: Boss 스프라이트 렌더 적용 (Add 2 나머지)

**Files:**
- Modify: `entities/Boss.js`

- [ ] **Step 1: Boss.js render() 수정**

`entities/Boss.js`의 `render(ctx)` 메서드:

```js
render(ctx) {
  import('../sprites/PixelRenderer.js').then(({ BOSS_SPRITE, PixelRenderer }) => {
    // 동기 import가 아니므로 직접 import 방식 변경 필요
  });
}
```

> 주의: Boss.js는 이미 PixelRenderer를 import하는지 확인 후, 아니면 상단에 추가.

`Boss.js` 상단에 import 추가:

```js
import { PixelRenderer, BOSS_SPRITE } from '../sprites/PixelRenderer.js';
```

`render(ctx)` 교체:

```js
render(ctx) {
  if (BOSS_SPRITE) {
    // phase에 따른 색상 tint
    PixelRenderer.drawSprite(ctx, BOSS_SPRITE, this.x - this.width / 2, this.y - this.height / 2, 2);
    // 2페이즈: 빨간 오버레이
    if (this.phase === 2) {
      ctx.save();
      ctx.globalAlpha = 0.3;
      ctx.fillStyle = '#cc0044';
      ctx.fillRect(this.x - this.width / 2, this.y - this.height / 2, this.width, this.height);
      ctx.restore();
    }
  } else {
    // 폴백: 기존 단색 렌더
    ctx.fillStyle = this.phase === 2 ? '#cc0066' : '#9900cc';
    ctx.fillRect(this.x - this.width / 2, this.y - this.height / 2, this.width, this.height);
  }

  // 피격 플래시
  if (this.hitFlashTimer > 0) {
    ctx.save();
    ctx.globalAlpha = 0.5;
    ctx.fillStyle = '#ffffff';
    ctx.fillRect(this.x - this.width / 2, this.y - this.height / 2, this.width, this.height);
    ctx.restore();
  }
}
```

- [ ] **Step 2: 전체 테스트 GREEN**

```
npx vitest run
```

- [ ] **Step 3: 커밋**

```bash
git add entities/Boss.js
git commit -m "feat: Boss 픽셀 스프라이트 적용 + 2페이즈 오버레이"
```

---

## 최종 검증

- [ ] **전체 테스트 GREEN 확인 (목표: 290개 이상)**

```
npx vitest run
```

- [ ] **브라우저에서 골든 패스 검증**

1. 인트로 → Space → 무기 선택 → 게임 시작
2. 버그가 항상 화면 밖에서 등장하는지 확인 (맵 중간 스폰 없음)
3. E1 이벤트 발생 → 새 모달 레이아웃 + 박진우 스프라이트 표시
4. IndentationError 15마리 처치 (특수 총알 발사 확인)
5. E3 이벤트 발생 → 이한정 스프라이트 표시
6. EnvError 돌진 공격 확인
7. E3 클리어 → 무기 획득 알림 표시
8. 보스 장선형 픽셀 스프라이트 등장 확인
9. 보스 50% HP → 2페이즈 (빨간 오버레이)
10. 보스 처치 → 무기 획득 알림 표시
11. 플레이어 사망 → gameOverLoop 안정 표시 + R키 재시작 잔상 없음

- [ ] **docs/phase-status.md 업데이트**

Phase 5 Cycle 1 (수정) + Cycle 2 (추가) 체크리스트 완료 표시

- [ ] **최종 커밋**

```bash
git add docs/phase-status.md
git commit -m "docs: Phase 5 Cycle 1+2 완료 표시"
```

---

## 자기 검토

**스펙 커버리지:**
- Fix 1 (WaveSystem 스폰) → Task 1 ✅
- Fix 2 (EventModal 크기) → Task 8 ✅
- Fix 3 (E3 무기 지급) → Task 10 ✅
- Fix 4 (게임오버 루프) → Task 2 ✅
- Add 1 (이벤트 화면) → Task 8 ✅
- Add 2 (보스 스프라이트) → Task 7 + 11 ✅
- Add 3 (NPC 스프라이트) → Task 7 ✅
- Add 4 (버그 체력 조정) → Task 3 ✅
- Add 5 (특수 공격 + 소환 수) → Task 5 + 6 ✅
- Add 6 (보상 무기 5종) → Task 9 + 10 ✅
- Add 7 (피격 효과) → Task 4 ✅

**타입 일관성:**
- `waveSystem.update(dt, playerX, playerY)` — Task 1에서 정의, main.js에서 동일 시그니처 사용 ✅
- `enemy.getAndClearPendingShots()` — Task 6에서 정의, main.js에서 동일 호출 ✅
- `GitWeapon.fire()` → `{isAreaEffect, radius, damage, cx, cy}` — Task 9a + 10 일치 ✅
- `LinuxBashWeapon.fire()` → `{isUlt, damage}` — Task 9e + 10 일치 ✅
- `EventModal.show(type, eventId, extra={})` — Task 8에서 정의, 기존 호출은 extra 없어도 동작 ✅
